// The blinking cursor is pure CSS (see .cursor / @keyframes blink in style.css)
// and already respects prefers-reduced-motion via the global rule there.
// This file is intentionally minimal — the site has no other behavior yet.
